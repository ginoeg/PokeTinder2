package tecsup.edu.poketinder.domain.model

data class Stats (

    val base_stat: Int,
    val stat: Stat

){/*
    fun StatsModel.toDomain()= Stats(
        base_stat=base_stat,
        //stat = stat.toDomain()
    )
    */
}